{
	"name": "moraine-web",
	"version": "0.1.0",
	"private": true,
	"license": "AGPL-3.0-or-later",
	"type": "module",
	"scripts": {
		"dev": "pnpm build:wasm && vite dev",
		"build": "pnpm build:wasm && vite build",
		"build:static": "pnpm build:wasm && MORAINE_WEB_TARGET=static vite build",
		"build:cloudflare": "pnpm build:wasm && MORAINE_WEB_TARGET=cloudflare vite build",
		"build:wasm": "cd ../wasm && wasm-pack build --target web --out-dir ../web/src/lib/signer/pkg --out-name moraine_wasm",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"lint": "oxlint",
		"lint:fix": "oxlint --fix",
		"fmt": "oxfmt",
		"fmt:check": "oxfmt --check",
		"test": "vitest run"
	},
	"dependencies": {
		"@lucide/svelte": "^1.47.0",
		"bits-ui": "^2.19.2",
		"zod": "^4.0.0"
	},
	"devDependencies": {
		"@sveltejs/adapter-cloudflare": "^7.0.0",
		"@sveltejs/adapter-static": "^3.0.0",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"@tailwindcss/vite": "^4.0.0",
		"@types/node": "^26.6.0",
		"daisyui": "^5.0.0",
		"oxfmt": "^0.68.0",
		"oxlint": "^1.83.0",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"tailwindcss": "^4.0.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16",
		"vitest": "^5.0.1"
	}
}
